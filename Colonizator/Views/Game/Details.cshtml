@model GameLogic.Game.Game

@{
}


<div>
    <h4>Игра @Model.Id</h4>
    <hr />

    @for (int i = 0; i < Model.Players.Count; i++)
    {
        @Html.ActionLink("В игру за " + Model.Players[i].Color, "Join", new { token = Model.Id, playerId = i })
        <hr />
    }
</div>